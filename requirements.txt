# ── Web 框架 ──────────────────────────────────
fastapi>=0.104
uvicorn[standard]>=0.24
sqlalchemy>=2.0
pydantic>=2.0
pydantic-settings>=2.0

# ── Agent 编排 + LLM ──────────────────────────
langgraph>=0.2
langchain-core>=0.3
langchain-openai>=0.2

# ── 可选 LLM Provider（按需取消注释） ──────────
# anthropic>=0.42       # 仅 llm.provider="anthropic" 时需要

# ── PDF 处理 ──────────────────────────────────
pymupdf>=1.24

# ── 数据源 ────────────────────────────────────
arxiv>=2.1
httpx>=0.27
beautifulsoup4>=4.12

# ── 向量检索 ──────────────────────────────────
chromadb>=0.5
sentence-transformers>=3.0

# ── 可选：网页搜索工具 ─────────────────────────
# tavily-python>=0.5   # 仅配置 TAVILY_API_KEY 时需要

# ── 定时任务 ──────────────────────────────────
apscheduler>=3.10

# ── 配置 ─────────────────────────────────────
pyyaml>=6.0
python-dotenv>=1.0
